# Plan: Fix Authentication & Build Forgot Password Flow

Here is the step-by-step plan to resolve your production issue and implement the new feature:

## 1. Fix the "Invalid origin" Error
* **Cause**: Better Auth is receiving requests from `https://isolatucasa.com`, but its `baseURL` is likely defaulting to `http://localhost:3000` (via `getBaseURL()` fallback), causing an origin mismatch.
* **Action**: We will update `lib/auth.ts` to include `trustedOrigins: ["https://isolatucasa.com", "http://0.0.0.0:3000"]`. This explicitly tells Better Auth to accept authentication requests from your production domain and local network IPs.

## 2. Implement the Forgot Password Flow
Since you already have `resend` installed and configured in `lib/actions/resend.ts`, we can seamlessly integrate it with Better Auth.

### Step 2.1: Update Auth Client (`lib/auth-client.ts`)
* Export the `forgetPassword` and `resetPassword` methods from your existing `createAuthClient` setup so they can be used in your React components.

### Step 2.2: Configure Email Sending (`lib/auth.ts`)
* Add the `sendResetPassword` callback to the `emailAndPassword` settings in your Better Auth configuration.
* Use the existing `Resend` setup to email the user a secure reset link generated by Better Auth.

### Step 2.3: Build "Forgot Password" Page (`app/(auth)/forgot-password/page.tsx`)
* Create a simple page with an email input form.
* When submitted, it will trigger `forgetPassword({ email, redirectTo: "/reset-password" })`.
* Show a success state ("Check your email for a reset link").

### Step 2.4: Build "Reset Password" Page (`app/(auth)/reset-password/page.tsx`)
* Create a page with "New Password" and "Confirm Password" inputs.
* Better Auth handles the secure token from the URL automatically.
* When submitted, it will trigger `resetPassword({ newPassword })` and redirect the user back to the login page.

### Step 2.5: Update Login Page (`app/(auth)/login/page.tsx`)
* Add a "Forgot your password?" link to the existing login form to complete the user journey.

---
**Ready to execute?** Click **Proceed** to authorize me to implement this plan.
